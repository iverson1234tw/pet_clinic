import 'package:flutter/material.dart';

class ClinicListPage extends StatelessWidget {
  const ClinicListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('這裡是動物醫院清單'),
    );
  }
}
